import TransferForm from "@/features/transfer-tokens/ui";
import Layout from "@/shared/ui/layout";

const Dashboard = () => {
  return (
    <Layout>
      <TransferForm />
    </Layout>
  );
};

export default Dashboard;
